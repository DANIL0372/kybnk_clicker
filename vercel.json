{
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python"
    }
  ],
  "rewrites": [{ "source": "/(.*)", "destination": "/app.py" }],

  "framework": "nextjs",
  "buildCommand": "npm run build",
  "outputDirectory": ".next"
}
